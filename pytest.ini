[pytest]
testpaths = tests
pythonpath = . tests
markers =
    engine_bug: asserts the correct reading, which the engine does not yet produce
    slow: rebuilds the whole corpus; opt in with --runslow
