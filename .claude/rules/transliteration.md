# Transliteration

Transliterate Arabic in plain ASCII with no diacritics — `idgham`, `silah`, `aridah`, `qalqalah`, `muqattaat`, `waqf` — never `idghām`, `ṣilah`, `ʿāriḍah`. Arabic script itself is fine in comments and docs when quoting an actual word or mark.
